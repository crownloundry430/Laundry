
  # Laundry app UI

  This is a code bundle for Laundry app UI. The original project is available at https://www.figma.com/design/JI60u9BtMcCi5VqtJ3TRPE/Laundry-app-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  